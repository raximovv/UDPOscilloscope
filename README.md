# UDPOscilloscope
This project simulates a real-time sensor streaming data over UDP. The sender generates a sine wave with optional noise, while the receiver displays it as a live oscilloscope waveform. It also uses sequence numbers to detect missing packets and calculate packet loss percentage. This mirrors real hardware communication systems like UART, CAN bus, and Ethernet sensor streaming.
